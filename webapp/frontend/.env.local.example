# API URL (default: /api, proxied by Next.js rewrites or Cloudflare Worker)
NEXT_PUBLIC_API_URL=/api

# Optional: Direct backend URL for server-side requests
# NEXT_PUBLIC_BACKEND_URL=http://localhost:8000/api

# App version (set automatically during CI/CD build, not needed locally)
# NEXT_PUBLIC_APP_VERSION=dev

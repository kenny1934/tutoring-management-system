# Zero Blast test suites

Three standalone Playwright scripts (not part of the Vitest/Playwright CI runners —
they need a static server and take ~1–2 min each):

| suite | checks | covers |
|---|---|---|
| `zb-solo-test.js` | 167 | solo run end-to-end: plan/points maths (incl. 1.5x double hit), root-rank pillar order, staging, collapse beats, camera, opt-in kind-6 hint (offered from the margin above the marking band) + half-fuse nudge, the cover's lesson-flow strip + split-layout fold check + the reclaim-behind-a-link tidy + its same-device hint, the kind-7 general-form gate (scripted x²+5x+6=2, the −2 trap nudge, finale double), report, and a phone-sized pass (stacking cover kicker, whole keypad above the fold) |
| `zb-multi-test.js` | 242 | host + 2 phones over a mocked GameBridge: lobby (lesson track + the track-following how-to card + the two-column fold and alignment checks), the phone's play-face layout (centred keypad in the thumb zone, progress line above the fold), kick (lobby + mid-game), claims, echo, grace, resume, tutor bar (+ rejoin QR chip), restart revival, re-scan identity, name dedupe, sent·marking beat; then 探究模式 (等式開口中): the three-stage inquiry arc, secret pairing, the 0×0 boom, the one hearts pool (no stage refill) and the rebuild bench at 0 hearts (+ the all-KO pity rule), the bridge's 分工 judging (each partner zeroes their OWN underlined factor; the partner's-root fail with its credit verdict; x(x−7)'s naked-x trap; the repeated-root tightening and its exception note; the 或-note), result cards led by the student's own line (你未提交 when they miss), the rotating bye on odd headcounts, tutor controls (加多一回合, stage jump; the jumps rest mid-round, nav cluster anchored right), host F5 recovery mid-arc, handover through the main-game briefing screen (its how-to, the phones' ready face with no echoed status or spent clock, the F5-safe briefing flag), the gate's trap verdict + finale double over the wire |
| `zb-audit-test.js` | 76 | EN/dark bilingual pass, small viewports (incl. the three-column keypad centred on screen), button icons that carry their own stroke, reduced motion, fx gates, sound sampler + offer, camera letterbox |

## Running

Serve the game statically, then point the suites at it:

```bash
# any static server over webapp/frontend/public — e.g.
cd webapp/frontend/public && python3 -m http.server 8000

# from the repo root
NODE_PATH=webapp/frontend/node_modules node webapp/frontend/tests/games/zero-blast/zb-solo-test.js
NODE_PATH=webapp/frontend/node_modules node webapp/frontend/tests/games/zero-blast/zb-multi-test.js
NODE_PATH=webapp/frontend/node_modules node webapp/frontend/tests/games/zero-blast/zb-audit-test.js
```

`ZB_BASE` overrides the target (default `http://localhost:8000/games/zero-blast/`).

## Harness conventions (why the suites look the way they do)

- **Deterministic levels** via `?seed=N`; single-stage runs use `?levels=K&rounds=1`
  (note: a single-stage run is treated as the finale street — double payouts).
- **Keypad submits** double-tap the same digit inside one `page.evaluate` — a digit
  arms and the same digit commits instantly, beating the 750ms commit window.
  Always wait for `G.staged` / `C.staged` first. (A *single* tap arms without
  committing — the audit suite uses that to probe the armed drain bar.)
- **Transient classes** (page turns, cracks, hit-stop, takeover, grace numerals) are
  caught with in-page MutationObserver latches armed *before* the trigger; polling
  from the test process loses the race.
- **Score odometers** tween ~900ms — settle before reading totals.
- **Warn/grace windows** are forced with `G.deadline = performance.now() + 5000`
  rather than waited for. 探究 rounds force `G.inqDeadline` the same way; the
  two-digit inquiry pad has **no** same-digit shortcut (taps append), so
  `inqSubmit` taps the digits once and waits for the sub write instead.
- The multi suite replaces `GameBridge.host/join` with a localStorage +
  BroadcastChannel mock that mirrors RTDB semantics; snapshots are captured at
  write time and delivered on a FIFO `setTimeout(0)` queue so nested writes never
  reorder events. No Firebase traffic is generated by any suite.

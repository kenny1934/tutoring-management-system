export const metadata = {
  title: "Summer Application | MathConcept Secondary Academy",
  icons: { icon: "/favicon-secondary.png" },
};

export default function ApplyLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}

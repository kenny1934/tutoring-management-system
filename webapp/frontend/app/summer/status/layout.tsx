export const metadata = {
  title: "Application Status | MathConcept Secondary Academy",
};

export default function StatusLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}

"""Curriculum concept parsing and path normalization.

Shared by the offline backfill scripts (database/curriculum/) and the backend
runtime (suggestion endpoint, flywheel classification of newly assigned PDFs).
"""

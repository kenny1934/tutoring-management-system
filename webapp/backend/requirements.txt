# FastAPI and ASGI server
# Pinned to the versions production ran on 13 September 2026. With only
# minimum versions, every image build installed the newest release, so
# production, CI and local venvs drifted apart without anyone choosing an
# upgrade. Starlette and anyio are pinned as well because they decide how
# requests are routed and how many plain def handlers run at once.
fastapi==0.141.1
uvicorn[standard]==0.52.4
starlette==1.6.0
anyio==4.15.1

# Database
sqlalchemy==2.0.25
pymysql==1.1.0
cryptography>=42.0.0

# Google Cloud SQL connector
cloud-sql-python-connector[pymysql]==1.7.0

# Authentication
google-auth>=2.27.0
google-auth-oauthlib>=1.2.0
python-jose[cryptography]==3.3.0

# Environment variables
python-dotenv==1.0.1

# CORS middleware
python-multipart==0.0.6

# Data validation
pydantic==2.13.5
pydantic-settings==2.15.0

# Utilities
python-dateutil==2.8.2

# Web Push notifications
pywebpush==2.3.0

# HTML sanitization (for rich text messages)
bleach>=6.0.0
tinycss2>=1.2.1

# Google Calendar API
google-api-python-client==2.115.0

# Google Cloud Storage (for inbox image uploads)
google-cloud-storage==2.14.0

# Image processing
Pillow==10.2.0

# HTTP client (used by paperless, wecom)
httpx>=0.27.0

# Google Generative AI (Gemini via Vertex AI)
google-genai>=1.51.0

# Document processing (optional - for handwriting removal)
numpy<2.0  # Required for opencv compatibility
opencv-python-headless==4.9.0.80
PyMuPDF==1.24.0

# Chinese Simplified → Traditional conversion (worksheet OCR)
opencc-python-reimplemented>=0.1.7

# Fuzzy string matching (prospect/application name similarity)
rapidfuzz>=3.0.0

# Revenue workbook generation (branch revenue sheet refresh)
openpyxl==3.1.5
